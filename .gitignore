build/
dist/
__pycache__/
*.pyc
*.egg-info/
.venv/
.venv-*/
*.wav
*.mp4
*.mp3
*.log
*.err
.ruff_cache/
ui/node_modules/
ui/dist/

# Local backups and generated slide exports kept out of git by default
_backup_files*/
slides_png/
voice_samples/

# Downloaded model weights (re-fetched on demand by the code)
assets/models/

# Loose scratch downloads / temp artifacts in the repo root (UUID pngs, zips, etc.)
/*.png
/*.zip
/sky_test.json
tmp/
chatgptsamples/*.zip

# Allow curated episode source/demo assets under content/ to be committed.
# Use this for small slide PNG decks and PDF decks that belong to an episode.
!content/**/*.png
!content/**/*.pdf
