"""Cutout 2D cartoon engine: separated-part character rigs animated programmatically
(walk/wave/point/blink/talk) over backgrounds, assembled into narrated MP4s.

Approach C (validated + research-backed): AI generates consistent part-art; Python
(Pillow + FFmpeg) animates deterministically; Shiksha-Cast assembles. No GPU at runtime.
"""
